@model IEnumerable<AngularView.Models.Sala>

@{
    ViewData["Title"] = "Salas";
    Layout = "_LayoutVendedor";
}

<h1>Salas</h1>


<div class="row">
    @foreach (var item in Model)
    {
        if (item.TipoSala == 1)
        {
            <div class="card col-3">
                <div class="card-header">
                    <h4>@item.Nombre <small> con @item.Caja.Count cajones</small> </h4>
                </div>
                <div class="card-body">

                    <a class="btn btn-outline-info" href="@Url.ActionLink("Salas","Salas")">Ver salas</a>
                </div>
            </div>
        }
        else
        {
            <div class="card  col-3" style="margin-right:1em; margin-left:1em;">
                <div class="card-header text-white bg-warning"> <h4>@item.Nombre <small> con @item.Caja.Where(e=>e.Ocupado==0).Count() cajones disponibles</small> </h4></div>
                <div class="card-body">

                    <a class="btn btn-warning" asp-action="Cajas" asp-controller="Salas" asp-route-id="@item.Id">Ver cajones</a>
                </div>
            </div>

        }
    }
</div>